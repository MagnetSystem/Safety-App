# Copy to .env.local for local dev. In production set this in the Vercel dashboard.
# Point at the deployed backend, including the /api suffix.
VITE_API_BASE_URL=http://localhost:3000/api

# Set to true only on non-production deploys if you want the demo credentials hint on login.
# VITE_SHOW_DEMO_LOGIN=true
